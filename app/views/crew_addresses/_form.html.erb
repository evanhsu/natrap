<%= form_for(@crew_address) do |f| %>
  <% if @crew_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crew_address.errors.count, "error") %> prohibited this crew_address from being saved:</h2>

      <ul>
      <% @crew_address.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :crew_id %><br />
    <%= f.text_field :crew_id %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_field :address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
